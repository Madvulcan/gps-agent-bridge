[Unit]
Description=GPS (Global Positioning System) Daemon - UDP mode for gpsdRelay
After=network.target

[Service]
Type=simple
ExecStart=/usr/sbin/gpsd -N -G -F /run/gpsd.sock udp://*:2948
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
